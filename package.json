{
  "name": "trigger-exp",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "db:migrate": "bun run --cwd packages/api tsx src/db/migrate.ts",
    "trigger:dev": "bun run --cwd packages/api dev:worker"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.8.0"
  },
  "packageManager": "bun@1.3.4"
}
